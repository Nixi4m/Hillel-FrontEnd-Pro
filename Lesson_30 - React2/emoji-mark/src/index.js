import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js'; // імпортуйте ваш кореневий компонент (наприклад, App)

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
